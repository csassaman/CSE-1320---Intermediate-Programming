- `my*var` is an invalid identifier because of the asterisk
- Write a function declaration for a function that determines if a given character is a consonant
    ```c++
    int is_consonant(char);
    ```
